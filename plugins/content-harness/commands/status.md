---
description: Report exact Free workspace jobs and packages without changing anything
---

Call `harness_status` with no arguments for the workspace list. If checking a previously returned job, use its exact `job` ID. To inspect a package, use its exact `packageId`. Do not substitute a requestId for a job ID. Report headline, package ID, current stage and next local browser action. Compare the saved brief from `harness_setup` with the user's request before calling any package current; label other packages as earlier work. Missing metrics are unknown. Pro services are unavailable, and publication is disabled.
